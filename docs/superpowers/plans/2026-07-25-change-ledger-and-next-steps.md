# 变更记录与下一步执行计划

> 更新日期：2026-07-25
>
> 依据：Git 历史、当前工作区差异、现有测试和两份调研/路线文档。
>
> 原则：先收口已开始的闭环与安全边界，再扩展主动运维功能；不把“采集中”数据用于考核或排名。

## 1. 当前基线

| 状态 | 内容 | 版本/位置 | 处理决定 |
| --- | --- | --- | --- |
| 已推送 | 到站巡检、必拍照片、离线本地优先同步 | `7bce661` | 保持，继续积累真实样本 |
| 已推送 | 运营基线接口与管理端基线视图 | `14bd491`、`4407e5b` | 保持；事件统计严格按所选周期过滤 |
| 已提交、未推送 | 审核打开/提交、报表打开/导出、行动队列进入埋点；基线按样本显示审核耗时与报表自助率 | `d571715` | 先完成回归、构建和真实管理员验证，再决定推送 |
| 未提交原型 | 影像 GPS/时间/异常项标红；正常照片一键通过；审核页标红置顶 | `平台开发/backend/app.py`、`react-vite/src/pages/audit/AuditPage.jsx` | 不能直接提交，先完成安全收口 |
| 未纳入版本控制 | 主动运维路线图、日常动线调研 | `docs/superpowers/plans/2026-07-25-*.md` | 作为参考输入；本计划是当前执行准绳 |
| 已确认删除 | `MEMORY补充.md` 的内容已并入主记忆文件 | 工作区 | 不恢复；删除可随本轮相关提交一并纳入版本管理 |

## 2. 已确认的度量范围

### 已采集

- 一线：到站打开、签到、拍照、离线暂存与同步。
- 管理端：审核打开/提交、评估报表打开/导出、行动队列深链进入。

### 当前展示规则

- 审核耗时：仅同一 `review_id` 的打开和提交事件配对后计算中位耗时。
- 报表自助率：`report.exported / report.opened`；打开样本为 0 时必须显示“采集中”。
- 行动队列：目前仅是进入次数样本，不得命名为“决策率”或用于管理评价。
- 任何指标未满真实基线观察期（建议 2–4 周）前，不设目标、排名或奖惩阈值。

## 3. 第一优先级：收口影像抽样审核原型

目标是“审核员只关注可疑照片，正常照片可批量通过”，但绝不能因为批量操作越权或误放行。

### P0：提交前必须完成

1. **权限与范围**：`auto-review` 必须按当前审核人的站点权限过滤；非管理员不能审批无权限站点的照片。接口响应应返回实际处理范围和数量。
2. **最小选择范围**：默认批量通过仅作用于当前筛选范围（至少支持站点范围）；前端操作前展示“将通过 N 张正常照片，标红 M 张保留”的确认信息。
3. **可追溯性**：批量通过写入审核人、审核时间、审核来源（如 `auto_pass_normal`）；保留逐张复核/驳回入口。
4. **上传路径矩阵**：逐一核对站点照片、巡检项照片、工单照片三类写入路径是否传入站点、关联项、GPS、拍摄时间；缺失元数据只能标记为“无法判定”，不能当作“正常”。
5. **历史回填保护**：`reevaluate` 仅管理员可执行，先提供预览统计或明确的二次确认；不得把回填作为启动时自动迁移。
6. **测试隔离**：将 `test_flag_e2e.py` 从真实 `data/water.db` 和硬编码账号中移出，改为临时 SQLite/Flask 测试客户端；禁止测试向运行库插入或删除记录。

### P1：完成 P0 后实施

1. 将标红规则从 `app.py` 抽为独立、可单测的模块；`app.py` 仅负责注册接口与调用。
2. 审核页按“标红优先 / 全部待审”提供筛选；批量操作只对当前可见且有权限的正常照片生效。
3. 对 GPS 偏离、未来时间、异常巡检项各自记录规则命中；支持多规则同时命中。
4. 增加审核质量看板：标红数量、自动通过数量、人工驳回数量，只展示样本量，不评价人员。

### 验收

- 普通审核员无法批量通过其他站点照片。
- GPS 偏离、未来时间、关联异常项三类照片均置顶并保留人工审核。
- 正常照片批量通过后可追溯到操作人和操作来源。
- 自动化测试不触碰 `data/water.db`；后端单测、接口测试、前端构建全部通过。

## 4. 第二优先级：验证并推送遥测基线提交

对 `d571715` 执行以下检查后再推送：

1. Python：`test_telemetry.py`、`test_operations_baseline.py`、`py_compile`。
2. 前端：`npm run build`；审核、评估、行动队列页面加载无阻断错误。
3. 管理员真实流程：打开审核并提交、打开并导出评估报表、从行动队列进入处理页；确认事件去重、时间格式归一化、所选周期过滤均有效。
4. 数据边界：不记录审核意见、照片、报表内容或精确地理位置以外的敏感上下文。

## 5. 后续主动运维路线（暂不开发）

在影像审核收口与遥测稳定后，按以下顺序启动独立设计和实施：

1. **巡检衔接层**：把人工上报/低风险告警转成下次计划的建议，而不是新增被动监测能力。
2. **物资嵌入执行链**：接单后、出发前确认车辆与备件；现场顺手记录试剂质控、异常上报、旧件回收，不新建脱离作业的“物资中心”。
3. **移动端异常上报**：GPS、照片、异常编码 → 管理端核实/归档 → 派生工单可视化闭环。

## 6. 版本管理规则

- 每一项功能单独提交；测试、计划与实现对应提交。
- 不混入运行数据库、上传文件、截图、个人记忆文件的删除或无关改动。
- 在推送前检查 `git diff --check`、测试/构建结果和真实接口验证。
- 当前不推送 `d571715`，也不提交影像审核原型，直到本计划第 3、4 节满足验收。
