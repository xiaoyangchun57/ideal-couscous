# 水质运维项目协作规范

本文件适用于本仓库全部目录。它只记录长期稳定、可执行、可验证的协作约束；当前版本号、临时端口、测试账号、某轮验收结果和一次性修复结论不得写入本文件。

详细业务规则见 `平台开发/docs/DEVELOPMENT_STANDARDS.md`，当前工作断点见 `平台开发/docs/PRODUCT_WORK_LEDGER.md`，产品复盘见 `平台开发/docs/PRODUCT_MANAGEMENT_RETROSPECTIVE.md`，运行方式见 `平台开发/README.md`，发布证据见 `平台开发/docs/RELEASE_TEST_HANDOFF.md`。本文件与用户当次明确要求冲突时，以用户要求为准。

## 一、先判断问题，再选择方案

1. 实施前先写清楚五件事：用户是谁、在哪个场景受阻、实际损失是什么、根因证据是什么、达到什么结果才算解决。
2. 严格区分现象、问题和方案。用户提出的方案是重要输入，但不是问题定义；可以反驳方案，但必须提供成本更低、闭环更完整的替代方案。
3. 先判断功能是否必要，再讨论如何实现。新增页面、状态、字段或后台操作前，必须证明它有独立责任人、独立动作或独立业务结果；否则优先合并、隐藏或删除。
4. 优先选择能闭环的最短路径。不得为了覆盖理论上的所有情况，把简单业务变成需要用户理解技术状态机的流程。
5. 先对齐业务与交互逻辑，再做视觉调整。设计目标应描述用户结果，例如“减少误操作”“缩短审核路径”“让失败可恢复”，不能只描述控件样式。
6. 修复必须覆盖问题本身，不得只遮住当前截图中的症状。发现同类组件或共享契约时应评估全局影响，但未经证据不得扩大为无关重构。
7. 诊断、方案评审和实施是不同授权阶段。用户要求“先复现、定位或评审”时保持只读，先交付复现证据、可能原因、涉及文件、待确认信息和最小修复计划；取得修改授权后才能写代码。
8. 当选择会改变责任归属、数据口径、审计风险或用户学习成本时，主任务应给出推荐方案、理由和替代项，请用户确认；不得把技术实现选择伪装成既定产品结论。

## 二、业务与安全不变量

1. 后端是权限、范围、状态迁移和有效数量的最终权威。前端隐藏按钮只能改善体验，不能代替服务端校验。
2. 权限按用户全部角色集合判断；数据范围还必须校验站点、组织和业务归属。管理员身份不自动等于现场执行人，审核权、管理权和现场操作权必须分开。
3. 改派执行人时同时校验目标人员角色和目标站点授权。任何直接 API、旧客户端或篡改请求都不得绕过该约束。
4. 状态迁移必须在事务内重新读取并校验当前状态、权限、资源和版本。批量操作先验证全部对象，再产生任何写入；失败时计划、事件、通知、任务、资源和附件均应零副作用。
5. 弱网重试和重复提交必须使用稳定幂等键。重复请求不得重复创建任务、审核项、通知、签到、资源占用或证据记录。
6. 审计记录和原始证据不可物理删除或静默改写。纠错采用作废、替换和追加审计；保留原文件、原因、操作者、时间及替换关系。
7. 不得用上传时间、创建时间或客户端自报值冒充可信拍摄时间。字段缺失应明确保持缺失，并在业务层给出可执行的恢复动作。
8. 位置打卡与距离范围必须由服务端复核。位置校准属于高风险写操作，必须显示对象与新旧坐标、二次确认并保留审计。
9. 影像来源可信由系统判断，审核人员只判断业务内容。技术判定不得扩张为需要人工维护的业务状态机。
10. 影像审核归属业务对象：巡检照片进入巡检质控，工单照片进入工单审核。无业务归属或来源不可信的照片可保留为补充材料，但不计有效证据、不进入主审核队列。
11. 补拍责任挂在检查项或工单业务项上，不挂在单张历史照片上。驳回只重开受影响业务项，保留其他已完成项、原签到和历史照片。
12. 当前有效档案与历史审计必须分开且口径互补。默认查询展示当前有效记录；驳回、作废、替换和补充材料仍可追溯，不得静默丢失。

## 三、实现边界

1. 修改前先阅读相关代码、测试、调用方和当前 `git diff`。工作区中的既有改动默认属于用户或其他任务，禁止回退、覆盖或顺手整理。
2. 采用满足目标的最小改动。未经明确批准，不新增依赖、不重构无关模块、不改公共契约、不批量格式化、不生成版本文件。
3. 优先复用项目现有组件、领域服务和结构化数据接口。禁止用脆弱的字符串拼接替代已有解析器或结构化字段。
4. 共享组件和全局 CSS 修改前必须列出所有调用方；无法证明全局适用时，使用页面或组件级作用域。
5. 异步请求必须处理过期响应、组件卸载、重复触发和取消。筛选、切页、切站等高频操作只能让最新请求更新界面。
6. 保存或提交失败时，保留用户输入与当前上下文，显示服务端可执行原因，并允许重试。禁止静默失败、只写控制台或失败后清空表单。
7. 类型、状态和时间字段必须保持单一语义。不得用一个字段推断另一条状态轴，也不得为兼容而无标签地回退为含义不同的字段。
8. 兼容旧客户端时可以继续解析旧请求，但服务端不得信任客户端自证的权限、数量、来源、位置或审核结果。

## 四、用户体验验收

1. Web 端是查询、监控和审核工作台，优先保证信息可扫描、筛选可持续、异常可定位、操作可恢复，而不是追求装饰性表现。
2. 页面必须明确区分加载、空数据、失败、有数据和列表结束。旧数据刷新失败时可保留旧数据，但必须提示其可能不是最新。
3. 工具栏、筛选框、标签、按钮和表格使用一致的高度、间距和对齐方式。输入框内外文本应在视觉基线上对齐；工具栏与内容区保持稳定间距。
4. 弹窗和抽屉在目标视口内不得出现标题、正文、计数器、表单和底部按钮重叠；长文件名、长原因和错误文本必须可换行且不遮挡操作。
5. 筛选只刷新其负责的数据区域，不阻塞整个页面；返回页面时应保留筛选、分页和必要的滚动上下文。
6. 地图、图表和重资源组件必须支持取消、关闭和快速切换。加载中的组件不能锁死退出路径，也不能在用户关闭后自动重开。
7. 移动端高频现场动作应在一至两次触达内完成，主操作位于拇指可达区；PC 审核流程应避免无意义跳页，并在当前上下文显示决策所需信息。
8. 所有可执行操作都必须有成功、失败、权限不足、对象过期和重复操作反馈。按钮禁用不能成为唯一解释。
9. 统计、列表、筛选和导出口径应使用同一集合；确需不同集合时，标题、说明和数量必须明确区分，不能让用户把两个正确数字理解为系统矛盾。
10. 界面显示业务名称和用户可理解的状态，不得直接暴露内部 ID、技术枚举、规则代号或模型置信度来代替业务含义。
11. 涉及共享布局、颜色或组件状态的改动，应同时检查目标桌面宽度、窄屏以及项目支持的明暗主题，避免只在单一主题或视口下成立。

## 五、验证与证据

1. 测试随风险分层执行：开发中先跑最小定向测试，模块完成后跑相关回归，只有准备冻结唯一候选时才跑一次最终全量门禁。不得为版本仪式反复运行无关全量测试。
2. 权限、共享状态机、资源占用和关键写操作至少覆盖：成功、越权、对象不存在、状态冲突、重复请求和中途失败回滚。
3. 修复历史绕过时，测试必须直接调用服务端端点，断言数据库零副作用；仅验证前端按钮隐藏不算安全测试。
4. React 逻辑变更运行相关测试、lint 和 build；后端运行相关 unittest 与 Python 编译；小程序运行 Node 测试和 JavaScript 语法检查。具体命令以仓库 README 和现有脚本为准。
5. UI 问题必须使用真实 UI 验收。单元测试、API 响应、DOM 静态检查和历史截图不能冒充当前 Web 或微信 UI 证据。
6. Web 布局至少检查 `1440x900` 和 `1024x768`；涉及移动端时检查真实窄屏。小程序业务或配置改变时，需在唯一标准项目入口中验证开发者工具，并按风险补真机验证。
7. 无法取得真实 UI 证据时明确标记 `NOT RUN` 或“未验证”，写清阻塞原因和剩余风险，不得推断为通过。
8. 测试只能使用隔离数据库、临时上传目录和独立端口。不得把生产数据、线上容器或用户正在使用的本地服务当测试夹具。
9. 启动服务前先检查端口和现有进程；只停止本任务启动的服务，保留用户已有微信开发者工具和其他任务进程。

## 六、协作与委派

1. 主任务持有问题定义、产品口径、范围、集成和最终验收权。开发经理或子任务不能自行改变业务口径、扩大范围或冻结版本。
2. 产品主任务保持业务代码只读，负责问题定义、产品口径、范围、工作台账、开发交接、只读 Review 和最终验收；开发经理是业务代码唯一写入者。产品文档和协作规范可由产品主任务维护，但不得借此修改业务实现。
3. 委派前必须提供：目标与原因、允许修改的文件或模块、禁止事项、输入契约、验收条件、测试要求和回传格式。代理不能完全理解这些内容时不得委派。
4. 复杂跨模块实现或产品判断不得交给仅适合机械任务的代理。更换模型或接管任务时，只继承代码现场和已验证事实，不自动继承前任判断。
5. 同一工作树同一时间只能有一个写入所有者。其他代理只能只读审查，避免并发覆盖和口径漂移。
6. 审查代理默认只读，并按严重性检查：现有功能回归、边界遗漏、错误处理、类型问题、安全风险和测试缺口。
7. 子任务回传必须包含：为什么做、改变的用户行为、涉及文件、测试证据、真实 UI 状态、残余边界和是否需要产品决策。测试数量不能替代这些说明。
8. 发现偏离时立即停止扩展，先整理“保留 / 修正 / 撤销”清单，再由主任务授权最小修正；不得在纠偏中继续叠加功能。
9. 主任务必须在 `平台开发/docs/PRODUCT_WORK_LEDGER.md` 维护唯一、持续的工作清单和当前断点。台账记录当前问题、状态、责任人、已验证证据、待决事项和下一动作；完成后压缩为结论，不把临时端口、账号、进程号或一次性测试数据写入长期规则。
10. 用户在执行期间补充事实、纠正口径或新增同范围缺陷时，默认只更新台账中对应事项的约束与验收条件，不得把补充消息误判为原任务结束。
11. 处理补充信息后，主任务必须自动恢复到原断点，继续完成实现、定向测试、真实 UI 验收和最终审查。只有用户明确要求“暂停”“停止”或“改做另一件事”时，才可替换或终止原主线。
12. 每次阶段性交付必须同时写明：本次吸收了什么补充、原主线当前进度、下一断点和仍未完成事项。不得只回答最后一条补充而遗漏此前已授权工作。

## 七、开发线与发布线

1. 开发线持续修改和隔离验证；部署线只接收已冻结的唯一候选。线上反馈回到开发线复现和修正，不直接在部署副本上长期开发。
2. 除非用户明确要求，不创建提交、标签、发布包，不推送、不部署，也不操作线上数据库或容器。
3. 一个候选只对应一个不可变提交和一个不可移动的注释标签。退回的标签仅保留历史追溯，禁止移动、复用或删除。
4. 冻结前先确认需求逐项闭环、定向测试通过、真实 UI 边界清楚，再执行最终全量门禁；不得先打标签再补证据或修改标签外文档冒充候选内容。
5. 发布包从冻结标签生成，只包含已提交源码；隔离数据库、测试照片、自动化缓存、私有 IDE 配置、密钥、日志和临时脚本必须排除。
6. 部署前独立确认域名与证书、环境变量、数据备份、迁移、健康检查和回滚方式。部署成功不等于产品验收通过。
7. 历史候选的测试结果和 UI 结论不能沿用为当前候选证据。每项证据必须注明对象、环境、时间和是否真实执行。

## 八、完成前最终审查

每次交付前逐项回答，存在未知时必须明确写出：

- 是否破坏现有功能或共享调用方？
- 是否遗漏空数据、弱网、重复操作、快速切换、并发、跨日、旧数据和旧客户端？
- 失败是否有反馈、保留输入、可重试且事务零副作用？
- 字段、状态、时间、ID、空值和序列化是否存在类型或语义问题？
- 是否存在越权、范围绕过、客户端自证、敏感信息泄露或不可逆数据风险？
- 是否补足与风险相称的自动测试和真实 UI 验收？

以上六项未完成，不得宣称“已修复”“可冻结”或“可部署”。
