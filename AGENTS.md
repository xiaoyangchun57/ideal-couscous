# 水质运维项目协作规范

本文件只保留长期稳定、可执行的硬约束。详细业务规则见开发树 `平台开发/docs/DEVELOPMENT_STANDARDS.md`；唯一产品断点固定在 `E:\杂七杂八\水质运维\平台开发\docs\PRODUCT_WORK_LEDGER.md`；开发执行只读取开发树内由该台账指定的自包含交接文件。产品复盘和发布证据仍留在产品树。用户当次明确要求优先。

## 一、工作树与职责

1. 唯一业务开发工作树是 `E:\杂七杂八\水质运维-开发线`；`E:\杂七杂八\水质运维` 只用于产品审查与发布侧工作，不承载未提交业务代码开发。
2. 每次实现、接管或委派前，先核对仓库根目录、分支、HEAD、`git worktree list` 和 `git status --short`。当前目录、分支名、相同 HEAD 或临时交接都不能替代工作树身份；不符合即停止。
3. 产品主任务负责问题定义、产品口径、范围、台账、只读 Review、验收口径、最终裁决和冻结候选交接；独立 UI 验收任务负责当前真实 UI 的问题复现、修正后验收和证据回传。开发经理是业务逻辑、接口适配和集成修正的唯一写入者；用户明确授权专业设计师直接实现前端时，设计师是该批已授权 UI 文件的唯一写入者，开发经理暂停重叠写入并在交接后只做业务契约与代码 Review。同一工作树不得并发写入。
4. 独立发布任务是“收到冻结版本到部署上线”的唯一执行者，负责交接校验、标签派生打包、生产预检、备份、部署、回滚验证和发布证据。产品主任务不再执行该阶段的打包、SSH 或生产操作。
5. 委派必须写清目标、允许范围、禁止事项、输入契约、验收条件、测试要求和回传格式；代理不能完整理解时不得委派。
6. 产品骨架、产品 Review、台账和检查点只存产品树；进入开发阶段后，唯一自包含开发交接必须存入开发树。开发交接不得要求开发经理到产品树拼接执行上下文，产品资料只作争议追溯。
7. 阶段性开发或设计任务必须通过自包含 Markdown 交接文件下达；对话只报告结论、文件路径和唯一下一动作，不用长文本替代交接文件。设计交接只限定问题、业务现状、文件范围、不可变边界和最终效果，具体视觉方案由专业设计师按其自有设计系统决定。
8. 交接文件是当前执行合同，不是过程档案。每次正式更新时同步删除已失效的旧指令、已闭环问题和重复证据；历史过程压缩为执行仍需知道的结论，不得靠连续追加章节无限增长。交接只保留当前目标、已确认基线、允许范围、不可变边界、验收、停点和必要回传格式。
9. 默认采用“轻量连续工作 + 边界门禁”。同一批次、同一范围和同一写入者下，用户后续反馈默认并入当前批次；只做简短确认，不新建交接、不重复环境校验、不重跑无关测试，也不因单项补充缩小原范围。需要交给执行者时再一次性更新现有交接。
10. 只有责任人或写入权切换、正式开发/设计交接、产品 Review、提交/冻结/发布，以及重启、上下文压缩或上下文不一致后的首次恢复，才执行完整上下文与边界检查。普通状态确认、解释和同轮连续调整不走完整流程。
11. 每个边界动作前只核对四项：当前完整范围、当前唯一写入者、当前唯一执行交接、当前唯一下一动作。四项未变化时继续原流程；任何一项变化时才更新台账、检查点或交接。`代码 Review PASS`、`设计静态 Review PASS` 与 `真实 UI PASS` 必须分别表述，不得互相替代。

## 二、判断与实现

1. 先区分现象、问题和方案，明确用户、场景、损失、根因证据和完成标准。可以反驳用户方案，但必须给出成本更低、闭环更完整的替代方案。
2. 优先选择能闭环的最短路径。新增页面、状态、字段或流程前，先证明其有独立责任、动作或业务结果；避免把简单业务变成用户需要理解的技术状态机。
3. 修改前阅读相关代码、测试、调用方和当前 diff。现有改动默认属于用户或其他任务，禁止回退、覆盖或顺手整理。
4. 采用满足目标的最小改动；未经批准，不新增依赖、不重构无关模块、不改公共契约、不批量格式化。
5. 保存或提交失败必须保留输入和上下文，显示可执行原因并允许重试；异步请求要处理过期响应、卸载、重复触发和取消。

## 三、业务与安全不变量

1. 服务端是权限、数据范围、状态迁移和有效数量的最终权威；前端隐藏或禁用不能替代服务端校验。
2. 权限按全部角色判断，并校验站点、组织和业务归属。管理员、审核员和现场执行人的权限不得互相替代。
3. 状态迁移在事务内重读权限、状态、资源和版本。批量操作先验证全部对象再写入，失败应零副作用；弱网重试使用稳定幂等键。
4. 客户端不得自证权限、数量、来源、位置或审核结果；不得用创建或上传时间冒充可信业务时间。
5. 审计和原始证据默认不可物理删除或静默改写；纠错优先采用作废、替换和追加审计。管理员按专门入口二次确认、填写原因并保留删除摘要后，可彻底删除已确认无效的计划、执行记录和影像；实际库存与车辆履约流水不得静默抹除。
6. 影像来源可信由系统判断，审核人员只判断业务内容；影像归属业务对象，无可信来源或无归属材料不计有效证据、不进入主审核。

## 四、验证与证据

1. 开发中只跑改动范围和直接相邻回归；只有冻结唯一候选时才跑一次最终全量门禁。
2. 权限、状态、资源和关键写操作至少覆盖成功、越权、不存在、状态冲突、重复请求和失败回滚；历史绕过必须直接调用服务端并断言零副作用。
3. React 变更运行相关测试、lint、build；后端运行相关 unittest 和 Python 编译；小程序运行相关 Node 测试和 JavaScript 语法检查。
4. 用户提供当前截图，且问题是单页面可见、无需角色差异、环境辨认、跨页面操作或下游业务结果即可判定时，截图直接作为复现证据，不再安排重复 UI 复现。其余 UI 问题及所有修正后验收由独立 UI 验收任务使用当前真实 UI 执行；产品主任务只定义口径、审阅证据和裁决。未执行时明确标记 `NOT RUN`，不能用单元测试、API、历史截图或静态检查代替。
5. 测试只使用隔离数据库、临时上传目录和独立端口；不触碰生产、固定数据库、线上容器或用户正在使用的服务。

## 五、连续性与发布

1. `E:\杂七杂八\水质运维\平台开发\docs\PRODUCT_WORK_LEDGER.md` 是唯一持续工作清单。开发树不得保留其内容副本；为兼容固定读取路径，开发树同名文件只允许保存只读指针和当前本地交接路径。用户补充或纠正同范围事实时，更新产品树对应约束并自动回到原断点；除非用户明确暂停、停止或改做其他事项，不得中断原主线。
2. 阶段交付需写明吸收的补充、当前进度、下一断点和未完成项。完成后把结论与证据索引写入台账，清理失效方案、重复诊断、孤立信息和废弃代理。
3. 清理上下文不等于删除代码、生产数据、审计、发布证据或用户文件。误写只能按显式白名单迁移，核对差异并通过定向测试后再清理源工作树。
4. 除非用户明确要求，不提交、不打标签、不打包、不推送、不部署，也不操作线上数据库或容器。
5. 一个候选对应一个不可变提交和不可移动标签；历史候选只保留追溯。发布包只从冻结标签生成，并排除测试数据、自动化缓存、私有配置、密钥、日志和临时脚本。
6. 面向用户的过程更新只报告产品动作、原因、结果和需要拍板的事项；工具调用、哈希、线程状态及重复验证细节仅在影响判断或用户明确询问时说明。

## 六、完成前检查

交付前明确回答：现有功能是否回归；边界是否遗漏；失败是否可见、可重试且零副作用；类型与状态语义是否一致；是否存在越权或不可逆风险；测试与真实 UI 证据是否与风险相称。存在未知时必须明确标注，不得宣称可冻结或可部署。
