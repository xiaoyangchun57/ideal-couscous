/* 顶部筛选已迁移至 app.wxss 全局 .seg-strip/.seg-item/.seg-on（margin 16/20 偏差随之统一） */

/* 列表行（.list-card/.row 族）与详情字段（.d-*）已提升至 app.wxss 全局定义 */

.sheet-ops { margin-top: 28rpx; }
.op-btn { width: 100%; }
.op-hint { text-align: center; color: var(--text-3); font-size: 26rpx; padding: 24rpx 0; }
