/* pages/plan/plan.wxss */

/* ===== 导航栏 ===== */
.nav-plan-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
  height: 100%;
}

.nav-plan-title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.scope-toggle {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx 16rpx;
  background: var(--muted);
  border-radius: 12rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
}

.scope-arrow {
  font-size: 20rpx;
  color: var(--text-tertiary);
}

/* ===== 范围切换中提示 ===== */
.scope-pending-bar {
  margin: 16rpx 32rpx 0;
  background: var(--accent);
  color: var(--primary);
  border-radius: 12rpx;
  padding: 14rpx 20rpx;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.spinner-mini {
  width: 24rpx;
  height: 24rpx;
  border: 3rpx solid rgba(0, 122, 255, 0.3);
  border-top-color: var(--primary);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* ===== 分段控件（对齐设计系统 segmented-control 规范） ===== */
.segment-wrap {
  padding: 16rpx 32rpx 12rpx;
  box-sizing: border-box;
}

.segment-control {
  display: flex;
  height: 64rpx;
  background: var(--color-type-bg);
  border-radius: var(--radius-sm);
  padding: 4rpx;
  box-sizing: border-box;
}

.segment-item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  font-size: 26rpx;
  color: var(--color-text-secondary);
  border-radius: calc(var(--radius-sm) - 2rpx);
  transition: all 0.2s ease;
  font-weight: 500;
  box-sizing: border-box;
}

.segment-item.active {
  background: var(--color-card);
  color: var(--color-text-primary);
  font-weight: 600;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
}

/* ===== 工具行 ===== */
.tools-row {
  display: flex;
  gap: 16rpx;
  padding: 0 32rpx 24rpx;
}

.tool-btn {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 10rpx;
  padding: 20rpx 20rpx;
  background: var(--card);
  border-radius: 16rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
}

.tool-btn-primary {
  background: var(--card);
}

.tool-btn-primary .tool-text {
  color: var(--text-primary);
  font-weight: 500;
}

.tool-btn-primary .tool-chevron {
  color: var(--text-quaternary);
}

.tool-icon-star {
  width: 28rpx;
  height: 28rpx;
  background: var(--primary);
  clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
  flex-shrink: 0;
}

.tool-btn-primary .tool-icon-star {
  background: var(--primary);
}

.tool-icon-plus {
  width: 28rpx;
  height: 28rpx;
  position: relative;
  flex-shrink: 0;
}
.tool-icon-plus::before,
.tool-icon-plus::after {
  content: '';
  position: absolute;
  background: var(--primary);
  border-radius: 2rpx;
}
.tool-icon-plus::before {
  top: 50%;
  left: 0;
  right: 0;
  height: 4rpx;
  transform: translateY(-50%);
}
.tool-icon-plus::after {
  left: 50%;
  top: 0;
  bottom: 0;
  width: 4rpx;
  transform: translateX(-50%);
}

.tool-btn-primary .tool-icon-plus::before,
.tool-btn-primary .tool-icon-plus::after {
  background: var(--primary);
}

.tool-text {
  flex: 1;
  font-size: 26rpx;
  color: var(--text-primary);
  font-weight: 500;
}

.tool-chevron {
  font-size: 32rpx;
  color: var(--text-quaternary);
  font-weight: 300;
}

/* ===== 刷新失败提示条 ===== */
.refresh-error-bar {
  margin: 0 32rpx 16rpx;
  background: rgba(255, 59, 48, 0.06);
  color: var(--destructive);
  border-radius: 12rpx;
  padding: 14rpx 20rpx;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.re-dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: var(--destructive);
  flex-shrink: 0;
}

.re-retry {
  margin-left: auto;
  font-weight: 600;
}

/* ===== 首次加载失败（对齐 empty-state error 变体） ===== */
.error-card {
  margin: 24rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 56rpx 32rpx;
  text-align: center;
}

.error-icon-shape {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  background: rgba(255, 59, 48, 0.12);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  margin-bottom: 20rpx;
}
.error-icon-shape::before {
  content: '!';
  font-size: 44rpx;
  font-weight: 600;
  color: var(--destructive);
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

.error-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 8rpx;
}

.error-subtitle {
  font-size: 26rpx;
  color: var(--text-secondary);
  line-height: 1.5;
}

.error-retry-btn {
  margin-top: 20rpx;
  font-size: 28rpx;
  font-weight: 500;
  color: var(--primary);
}

/* ===== 计划卡片列表 ===== */
.plan-list {
  /* 卡片左右边距由 .card 的 margin 控制 */
}

.plan-card-item {
  padding: 28rpx;
  margin-bottom: 20rpx;
}

.card-tags {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.card-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-top: 16rpx;
  display: block;
  line-height: 1.3;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-meta {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  margin-top: 10rpx;
  gap: 0 12rpx;
}

.meta-item {
  font-size: 24rpx;
  color: var(--text-secondary);
}

.meta-dot {
  font-size: 24rpx;
  color: var(--text-quaternary);
}

.card-status-note {
  font-size: 26rpx;
  line-height: 1.4;
  margin-top: 12rpx;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.note-neutral { color: var(--text-tertiary); }
.note-info { color: var(--primary); }
.note-warning { color: var(--warning); }
.note-success { color: var(--success); }

.card-actions {
  display: flex;
  align-items: center;
  margin-top: 20rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid var(--border);
}

.action-link {
  display: flex;
  align-items: center;
  gap: 4rpx;
  font-size: 26rpx;
  color: var(--text-secondary);
}

.action-link-primary {
  color: var(--primary);
  font-weight: 500;
}

.link-chevron {
  font-size: 28rpx;
  font-weight: 300;
  color: var(--text-quaternary);
}

.action-link-primary .link-chevron {
  color: var(--primary);
  opacity: 0.6;
}

.action-delete {
  margin-left: auto;
  font-size: 26rpx;
  color: var(--destructive);
}

/* ===== 列表空态 ===== */
.list-empty {
  padding: 80rpx 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.empty-icon {
  width: 96rpx;
  height: 96rpx;
  margin-bottom: 24rpx;
  flex-shrink: 0;
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
}

.empty-icon-doc {
  background-image: url('data:image/svg+xml;base64,');
}

.empty-icon-filter {
  background-image: url('data:image/svg+xml;base64,');
}

.empty-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 12rpx;
}

.empty-desc {
  font-size: 26rpx;
  color: var(--text-tertiary);
  line-height: 1.5;
}

/* ===== 骨架屏 ===== */
.loading-skeleton {
  padding: 0 32rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.skeleton-card {
  height: 200rpx;
  background: var(--card);
  border-radius: 24rpx;
  animation: shimmer 1.5s ease-in-out infinite;
}

@keyframes shimmer {
  0% { opacity: 0.6; }
  50% { opacity: 1; }
  100% { opacity: 0.6; }
}

/* ===== Sheet 弹层 ===== */
.sheet-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 100;
}

.sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: #ffffff;
  border-radius: 28rpx 28rpx 0 0;
  padding: 12rpx 32rpx calc(36rpx + env(safe-area-inset-bottom));
  z-index: 101;
  animation: slideUp 0.25s ease-out;
  max-height: 84vh;
  overflow-y: auto;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.sheet-handle {
  width: 72rpx;
  height: 8rpx;
  background: #e5e5ea;
  border-radius: 4rpx;
  margin: 0 auto 24rpx;
}

.sheet-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 28rpx;
}

.sheet-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.sheet-close {
  font-size: 40rpx;
  color: var(--text-tertiary);
  line-height: 1;
  padding: 8rpx;
}

.picker-field {
  margin-bottom: 20rpx;
}

.field-label {
  font-size: 24rpx;
  color: var(--text-tertiary);
  margin-bottom: 10rpx;
  display: block;
}

.input-field {
  background: var(--muted);
  border-radius: 16rpx;
  padding: 24rpx 28rpx;
  font-size: 28rpx;
  color: var(--text-primary);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.picker-arrow {
  font-size: 24rpx;
  color: var(--text-tertiary);
}

.form-row {
  margin-bottom: 8rpx;
}

.sheet-error {
  background: rgba(255, 59, 48, 0.06);
  border-radius: 12rpx;
  padding: 16rpx 20rpx;
  margin: 20rpx 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.error-text {
  font-size: 24rpx;
  color: var(--destructive);
}

.error-retry {
  font-size: 24rpx;
  color: var(--destructive);
  font-weight: 600;
}

.sheet-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 28rpx;
}

.sheet-actions .btn-ghost {
  flex: 1;
  height: 96rpx;
  border-radius: 24rpx;
  font-size: 30rpx;
  font-weight: 500;
}

.sheet-actions .btn-primary {
  flex: 2;
  height: 96rpx;
  border-radius: 24rpx;
  font-size: 30rpx;
  font-weight: 500;
}

/* PM-15 favorite-plan sheet: one scroll surface and one safe-area owner. */
.plan-favorite-sheet {
  background: #ffffff;
  border-radius: 28rpx 28rpx 0 0;
}
.plan-favorite-scroll {
  padding: 12rpx 32rpx 24rpx;
  overflow-y: hidden;
  box-sizing: border-box;
}
.plan-favorite-heading {
  padding: 12rpx 104rpx 24rpx 0;
}
.plan-favorite-close .ico-close::before,
.plan-favorite-close .ico-close::after {
  background: var(--text-tertiary);
}
.plan-favorite-sheet .sheet-actions {
  margin: 0;
  padding: 20rpx 32rpx calc(20rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--border);
}

.btn-danger {
  color: var(--destructive);
  border: 1rpx solid rgba(255, 59, 48, 0.3);
}

/* ============================================================
 *  常用计划 Sheet · 快速选择/轻操作类
 *  设计系统：水质运维-Apple
 *  类型特征：字段少、操作明确、Sheet 高度紧凑
 * ============================================================ */

/* --- Sheet 容器 --- */
.plan-favorite-sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--color-card);
  border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  z-index: 101;
  max-height: 85vh;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

/* --- 关闭按钮（绝对定位右上角） --- */
.plan-favorite-close {
  position: absolute;
  top: 16rpx;
  right: 16rpx;
  z-index: 2;
  min-width: 88rpx;
  min-height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.plan-favorite-close .ico-close::before,
.plan-favorite-close .ico-close::after {
  background: var(--color-text-tertiary);
}

/* --- 滚动正文区 ---
   flex: 0 1 auto：内容少时由内容撑开，Sheet 紧凑；
   内容多时被 max-height 限制，flex-shrink 压缩并内部滚动 */
.plan-favorite-scroll {
  flex: 0 1 auto;
  min-height: 0;
  overflow-y: auto;
  padding: 28rpx 40rpx 8rpx;
  box-sizing: border-box;
}

/* --- 标题区 --- */
.plan-favorite-heading {
  margin-bottom: 24rpx;
  padding-right: 80rpx; /* 避让右上角关闭按钮 */
}

.plan-favorite-heading .sheet-title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--color-text-primary);
  line-height: 1.3;
}

/* --- 表单字段 --- */
.plan-favorite-sheet .picker-field,
.plan-favorite-sheet .form-row {
  margin-bottom: 20rpx;
}

.plan-favorite-sheet .field-label {
  display: block;
  font-size: 24rpx;
  color: var(--color-text-tertiary);
  margin-bottom: 10rpx;
  font-weight: 500;
  line-height: 1.4;
}

.plan-favorite-sheet .input-field {
  background: var(--color-background);
  border-radius: var(--radius-md);
  padding: 0 24rpx;
  height: 88rpx;
  font-size: 28rpx;
  color: var(--color-text-primary);
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-weight: 500;
  box-sizing: border-box;
}

.plan-favorite-sheet .picker-arrow {
  font-size: 24rpx;
  color: var(--color-icon-tertiary);
  flex-shrink: 0;
  margin-left: 12rpx;
}

/* --- 错误提示条 --- */
.plan-favorite-sheet .sheet-error {
  background: var(--color-error-surface);
  border-radius: var(--radius-md);
  padding: 16rpx 20rpx;
  margin: 8rpx 0 16rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.plan-favorite-sheet .error-text {
  font-size: 24rpx;
  color: var(--color-error);
  line-height: 1.4;
  flex: 1;
}

.plan-favorite-sheet .error-retry {
  font-size: 24rpx;
  color: var(--color-error);
  font-weight: 600;
  flex-shrink: 0;
}

/* --- 底部操作栏 ---
   双按钮：次按钮占 1 份，主按钮占 2 份
   安全区仅在此处加一次，Sheet 容器不加 */
.plan-favorite-sheet .sheet-actions {
  display: flex;
  gap: 16rpx;
  padding: 12rpx 40rpx;
  padding-bottom: calc(12rpx + env(safe-area-inset-bottom));
  border-top: 0.5rpx solid var(--color-divider);
  background: var(--color-card);
  flex-shrink: 0;
  margin: 0;
}

.plan-favorite-sheet .sheet-actions .btn-ghost {
  flex: 1;
  min-height: 88rpx;
  line-height: 88rpx;
  height: auto;
  border-radius: var(--radius-lg);
  font-size: 28rpx;
  font-weight: 500;
  background: var(--color-background);
  color: var(--color-text-secondary);
  border: none;
}

.plan-favorite-sheet .sheet-actions .btn-ghost::after {
  border: none;
}

.plan-favorite-sheet .sheet-actions .btn-danger {
  color: var(--color-error);
}

.plan-favorite-sheet .sheet-actions .btn-primary {
  flex: 2;
  min-height: 88rpx;
  line-height: 88rpx;
  height: auto;
  border-radius: var(--radius-lg);
  font-size: 28rpx;
  font-weight: 500;
  background: var(--color-primary);
  color: var(--color-primary-foreground);
  border: none;
}

.plan-favorite-sheet .sheet-actions .btn-primary::after {
  border: none;
}

/* ============================================================
 *  覆盖全局 .om-sheet 固定高度：快速选择/轻操作内容驱动紧凑化
 *  产品裁决 8.4：仅常用计划紧凑化，详情/输入类保持 88vh 高弹层
 * ============================================================ */

/* 根容器：height:auto + max-height 替代全局固定 88vh，内容少时紧凑
   选择器：.om-sheet.plan-favorite-sheet 双类（特异性 0,2,0）> 全局单类（0,1,0） */
.om-sheet.plan-favorite-sheet {
  height: auto;
  max-height: 65vh;
}

/* 滚动层：flex-basis:auto 让高度随内容，min-height:0 允许 flex 收缩时 scroll-view 接管滚动
   选择器：4 个类（特异性 0,4,0）> 全局 .om-sheet.om-sheet .om-sheet-scroll（0,3,0）
   不修改 overflow-y，单滚动责任仍由原生 scroll-view 承担 */
.om-sheet.plan-favorite-sheet .om-sheet-scroll.plan-favorite-scroll {
  flex: 1 1 auto;
  height: auto;
  min-height: 0;
}

/* ===== tabBar 占位 ===== */
.tabbar-spacer {
  height: calc(120rpx + env(safe-area-inset-bottom));
}
