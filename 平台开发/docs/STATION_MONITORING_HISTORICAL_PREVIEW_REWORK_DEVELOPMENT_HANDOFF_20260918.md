# 站点监测历史数据业务投影返修交接

> 状态：`PRODUCT_REVIEW_PASS / FREEZE_AUTHORIZED`
> 基线：`codex/next-development` / `55c026572f9c61e0a5fc80b7bf9cd8ddc075d0da`
> 写入状态：开发已停止；本批已获准提交和冻结，未部署，生产接收器保持停止

## 结论

本批代码 Review 通过。后补周期后的幂等投影、迟到有效值、可疑值排除、有效覆盖率、有效站点摘要及前端展示口径已经统一；`HJ212:005` 与 `w01009` 保留协议码并合并为溶解氧业务因子。

自动验证：后端 82 项、前端 90 项、lint/build、`git diff --check` 全部通过。

## 本地真实数据对照

隔离回放 1,452 条脱敏 `CN=2011` 报文，得到 860 个唯一分钟批次；按四小时业务时点形成 4 个时点 × 9 个有效因子，共 36 条当前业务观测。页面/API 的 9 个最新有效值与数据库逐项一致；历史趋势接口返回 4 个正式时点，`valid_points=4`、`expected_points=4`、覆盖率 100%，不再展示 860 个分钟点。溶解氧、单位、观测/通信分轴和页面纵向滚动均已验证。

真实 UI 页面加载与现有数据呈现 `PASS`。历史曲线图本身 `NOT RUN`：历史样本已超出页面固定“最近24小时”窗口，页面显示无近期有效观测是正确结果，不篡改业务时间制造证据。

## 保留边界

生产数据库、接收器、配置和公网入口均未操作；归档、清理和 Excel 导出不在本批。真实曲线图 `NOT RUN` 已由产品接受为冻结残余边界；冻结后唯一下一步是交由独立发布任务评估并执行部署，发布前不得恢复生产接收器。
