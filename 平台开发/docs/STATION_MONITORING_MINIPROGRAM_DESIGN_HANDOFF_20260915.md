# 站点监测与首页入口图标设计交接

> 状态：`DESIGN_REVIEW_PASS + REAL_UI_PASS / CLOSED`
> 写入状态：设计师与开发经理均已停止本批写入。
> 停点：本批已闭环；可按 `STATION_MONITORING_DUAL_CLIENT_DEVELOPMENT_HANDOFF_20260912.md` 纳入白名单检查点提交，不推送、不冻结、不发布。

## 开发已提供的事实

- 页面标题已经改为“站点监测”，首次固定加载 `mine`。
- 服务端返回可用范围、本人/全部数量和每站 `is_responsible`；仅管理员具备 `all`。
- 当前 WXML 已接入范围切换、全部站点的名称/编号搜索、清空、本人标识及不同空态，事件和异步保护已通过 Code Review。
- 设计师只处理最终呈现，不补权限、不计算数量、不改检索或跳转逻辑。

## 待设计问题

1. 管理员的站点监测页将新增“我负责 / 全部站点”范围、本人负责标识和站点名称/编号检索。开发侧先提供服务端权威事实与可用节点；设计师随后按现有设计系统完成信息层级、范围辨认、检索及各空态的最终呈现。
2. 首页顶部消息通知图标由独立 PNG 移植并放大后，在真机上明显失真。需按现有设计系统重新选择或制作适配该位置的清晰图标，不沿用当前失真资产。
3. 首页“全部工单”入口目前使用 WXML/CSS 拼接的文档形状，与现有设计系统的图标语言不一致。需按设计系统替换，不能继续保留独立手绘样式。

## 基本边界

- 站点监测默认“我负责”；只有服务端授权管理员时才有“全部站点”。搜索只覆盖站点名称和站点编号，不展示 MN。
- 本人负责站点必须可辨认，但具体标签、布局和视觉层级由设计师决定。
- 不改变监测状态、详情跳转、导航、位置校准、请求与权限语义；已通过的站点详情页不重做。
- 首页消息图标必须保持 `goMessages` 入口、未读角标及现有点击热区，不改变消息数量、加载或跳转逻辑。
- “全部工单”必须保持 `goWorkorder`、列表态目标、标题和说明语义，不改变权限、路由或交互层级。
- 两个新图标在真机实际显示尺寸下应清晰，并与首页和全局设计系统一致；具体资产格式、线条和尺寸由设计师决定。
- 不修改 JS、API、后端、全局业务逻辑或其他页面。

## 预计允许范围

- `平台开发/miniprogram/pages/responsible-sites/responsible-sites.wxml`
- `平台开发/miniprogram/pages/responsible-sites/responsible-sites.wxss`
- `平台开发/miniprogram/pages/responsible-sites/responsible-sites.json`（仅已接线标题需要时）
- `平台开发/miniprogram/pages/index/index.wxml`
- `平台开发/miniprogram/pages/index/index.wxss`
- 首页消息图标对应的 `平台开发/miniprogram/images/` 资产

完成后回传实际修改文件、三个问题的收口结果和静态检查。真实小程序 UI 标记 `NOT RUN`。

## 产品设计 Review

`PASS`。站点范围已按服务端能力形成“我负责 / 全部站点”分段、全部站点检索、本人标识及区分后的空态，原事件绑定未改变；首页消息入口改用与现有设计系统一致的矢量图形并保留未读角标和点击热区；“全部工单”移除 CSS 拼图，改用统一矢量图标并保留 `goWorkorder`。

小程序范围/搜索、首页状态和消息目标直接测试 PASS，定向 `diff-check` PASS。真实小程序 UI `NOT RUN`；需在真机确认消息图标清晰度，并在管理员和普通人员账号下确认范围、搜索与本人标识的实际呈现。

## 真实 UI 验收

`PASS`。使用方已在当前真实小程序完成管理员/普通人员范围、全部站点搜索与清空、本人负责标识，以及首页消息和“全部工单”图标验证；本批关闭。真实站点数据接入后的数值与加载表现不在本次验收结论内。
