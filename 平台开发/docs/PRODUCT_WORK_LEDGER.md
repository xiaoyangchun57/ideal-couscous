# 产品工作台账

> 更新时间：2026-08-19
> 用途：记录当前主线、断点、责任人和证据。它是唯一活跃工作清单，不是长期业务规范或发布证明。

## 当前主线

r9 已上线，当前唯一开发主线是八项体验收口：计划日期与未来计划、现场页与本地接口、常用计划与异常复查、日期×站点任务详情、中文导入导出与工作报告、检查项频次和设备适配、无效计划/工单清理、到期建议退役兼容。

业务代码已按分层完成定向实现，后端、小程序、React 代码层经产品分段只读 Review 暂无已知代码阻断。`python-docx` 是本轮唯一明确授权的新增依赖，已同步正式依赖清单，依赖项 CLOSED。产品已确认常规到期巡检建议属于假性需求：站点和例行工作固定，系统责任收口为计划、执行、证据、审批记录和电子资料整理；系统性异常复查由异常事实触发，继续保留。2026-08-18 的计划变更验收使用开发线客户端与线上 r9 API 的混合版本，只能证明新客户端对 r9 的兼容。后续只保留开发线和部署线两条工作线：开发线承担业务实现、定向测试、本地服务和发布前 Web/小程序真实 UI 验收，部署线只承担冻结后的不可变提交与标签、备份、打包、部署和回滚。发布前 UI 不推迟到体验版；体验版只用于少量真实用户补充验证。日期编辑器、计划列表状态筛选、日期×站点任务详情/总体状态、统一审核、到期建议退役后的小程序和设备规则退役后的 Web 设置页等已明确列出的局部交互完成开发线发布前真实 UI 验证并 `PASS`；无效数据清理只验证当前真实库空态，三类排程人原因卡片仍为 `NOT RUN`，下载入口及 DOCX/XLSX 文档视觉为 `SKIPPED`。r10 最终自动门禁、精确暂存及 cached/candidate/index 边界检查均已通过，当前已形成冻结候选内容边界，但尚未创建不可变提交或标签；这些局部 UI 和自动结果不代表八项整体 UI `PASS`，也不得据此宣称可部署或已部署。

## 固定职责与边界

- 产品主任务：维护问题定义、产品口径、范围、台账口径、只读 Review 和最终验收；业务代码只读。
- 开发经理：唯一开发线业务代码写入者，只做八项及直接相邻契约，不执行版本或线上操作。
- 不新增调度框架、建议系统、报表引擎、清理状态机或历史批量迁移；不触碰 private、TEST_UI、automation、minitest、历史源码包和用户服务。
- 真实 UI 未执行时只能写 `NOT RUN`，自动测试、API、结构重开或渲染文件不能替代真实交互。
- 只保留开发线和部署线两条工作线；开发线承担业务实现、定向测试、本地服务及发布前 Web/小程序真实 UI，部署线只承担冻结后的不可变提交与标签、备份、打包、部署和回滚。
- 标准本地端口统一为后端 `5000`、Web `5174`，使用现有 `vite.config.js` 和一个微信开发者工具项目；端口冲突时启用的临时端口只是临时进程，不构成新环境，用完即停止并删除临时数据库和配置。
- r1-r9 只作 Git 追溯，不是活动环境；体验版只用于少量真实用户补充验证，不能替代开发线发布前真实 UI 验收。
- 需要用户参与验收时，产品主任务必须主动提供入口、账号或测试数据、操作步骤、预期结果、是否写数据及恢复方法。

## 八项状态

| 项目 | 当前代码状态 | 兼容边界与待验收 |
| --- | --- | --- |
| 1. 日期与未来计划 | create/update/validate 共用严格日期契约；支持非整周自定义日期；缩周期仅保护有站点、备注、检查项或车辆的真实日期，旧空周骨架内存忽略；首页提供独立即将执行摘要 | 周检范围驱动和季检显式执行日的本轮真实 UI `PASS`；不增加时分或调度器；未来计划不可提前签到/执行；跨期、变更和到日切换等其余边界仍未全部验证 |
| 2. 现场页与本地接口 | 删除顶部静态负责站点；无今日任务改为中性可执行提示；本地 override 有过期/清理和明确错误；local 写失败不进线上重放队列，队列按 profile/account 隔离 | 线上默认 URL 不变；不自动回退生产凭据；项目唯一入口保持父目录；需发布前真机验证登录与网络提示 |
| 3. 常用计划与异常复查 | 常用计划继续生成可编辑草稿；系统性异常复查继续按异常事实生成建议，负责人可建复查草稿，管理员可查看团队复查并通知负责人 | 不恢复常规到期提醒或自动造草稿；异常复查 GET/POST、`inspection_follow_up_suggestion` 与站点优先级建议保持独立 |
| 4. 已生成任务与详情 | 用户数量按日期×站点；总体 `execution_status` 只读派生为待执行、部分完成、已完成、变更待审，列表以固定一次批量聚合取得各计划完成度；详情优先展示日期×站点任务；移除重复原始任务表；逾期动作按内部当日包仅显示一次并在事务内拒绝非 approved 父计划 | 内部 insp_plan/field_status 模型不变，旧字段及 r9 `generated_plans` 仅作兼容 fallback；日级关闭影响该日包全部站点，界面明确显示站点数；日期×站点、总体状态、`change_submitted` 执行门禁及计划列表状态筛选的开发线发布前真实 UI `PASS` |
| 5. 中文导入导出与报告 | 站点/设备中文模板、中文多 sheet 站点档案、固定结构工作报告 DOCX 和详细数据 XLSX 已实现；人员绩效长表头列宽已修复 | 历史英文 CSV/XLSX 继续兼容；唯一新增依赖 `python-docx` 已获授权并登记，依赖项 CLOSED；下载入口及 DOCX/XLSX 视觉检查按产品决定本轮 `SKIPPED`，不是 PASS，也不构成候选验收或冻结阻断 |
| 6. 检查项频次与适用性 | 新写入仅 weekly/monthly/quarterly/yearly；annual 编辑安全归一 yearly；废弃值显示需管理员维护且不自动匹配；设备适用规则已退役，计划按频次取得全部有效检查项后由人员勾选 | 不批量回写 daily/semi_annual/high/mid/low；Web 设置页、模板弹窗和检查项弹窗无设备规则、类型匹配或高中低频次，周/月中文，真实 UI `PASS`；未执行保存、删除或审批；match 无只读 UI 入口，服务端和生成测试为证据 |
| 7. 无效计划/工单清理 | 复用 candidate/preview/apply；识别逾期无效站点、撤权及排程人不存在/停用/失去 operator 且无现场事实的计划；签到、定位、附件、审核、结果、资源等任一事实均保护；批量事务内重算，变化整批 409 零写入 | 只有未提交无事实草稿可物理删除；其他无事实对象只异常关闭并追加审计；代码 Review `PASS`。Web 弹窗可打开、无报错、空态正确且确认按钮禁用，未执行清理；真实库无自然候选，三类排程人原因卡片文案 `NOT RUN`，不为验收造数据 |
| 8. 到期建议退役兼容 | Web/小程序已移除到期建议入口、请求和动作；旧 GET/POST 稳定返回 `410 INSPECTION_DUE_SUGGESTION_RETIRED`；定时注册及专用候选实现已移除 | 小程序万松账号直接显示两张实际计划；常规到期建议、本人/团队到期建议切换和生成草稿动作均消失；异常复查建议保留；控制台无新增业务错误，真实 UI `PASS` |

## 分层证据

- 后端：最终完整 discover 365/365 `OK`（83.068s）。首次完整运行的 2 项失败均为测试契约过期：多角色内存 fixture 缺生产必备 `insp_plans`/`insp_plan_items`，旧缩周期测试错误期待静默裁剪；仅修测试后开发定向 64/64、产品独立 3/3、最终完整 365/365 通过，业务代码未因门禁失败修改。附件 void 与工单图片上传的 HTTP 500 日志为主动故障注入，回滚断言通过。
- 小程序：全部 23 个测试文件 51/51 `PASS`；全目录 62 个 JavaScript 文件 `node --check` `PASS`。既有日期×站点、总体状态、变更待审门禁及到期建议退役等已记录真实 UI `PASS` 继续有效。
- React：全部 13 个测试文件 71/71 `PASS`；ESLint `PASS`；production build `PASS`（1748 modules）。既有统一审核站点统计、变更待审展示及相关已记录真实 UI `PASS` 继续有效。
- 导出：隔离 Flask 夹具生成 1 个 DOCX 和 5 个 XLSX；MIME、中文 `Content-Disposition`、扩展名均正确。详细报告、两类模板和站点档案共 4 个 XLSX / 16 个工作表经 bundled spreadsheet runtime 导入和逐 sheet PNG 检查；独立人员指标 XLSX 的 1 个工作表由产品独立重开并渲染，修后表头清晰。合计 5 个 XLSX / 17 个工作表结构与可读性 PASS；DOCX 可重开为 18 段、1 表，固定八章节齐全。
- 依赖：`python-docx` 为本轮唯一授权新增依赖，已写入正式 `backend/requirements.txt`，依赖授权与登记 CLOSED。
- 语法与一致性：backend 全部 75 个 Python 文件语法 `PASS`；`python-docx 1.2.0` 导入烟测 `PASS`；`git diff --check` 0 个空白错误，仅现有 LF→CRLF 提示。
- 冻结索引：检查时精确暂存 60 个文件，`git diff --cached --check` exit 0（仅全局 ignore 权限 warning），candidate guard 2/2 `OK`；cached count 60、unstaged content 0，全部 cached 路径均位于 `平台开发`，禁入路径 0。唯一未跟踪项为根 `project.private.config.json`；`miniprogram/services/maps.js` 工作树过滤后 hash 与 index 一致且未暂存。
- DOCX 视觉限制：官方 `render_docx.py` 因本机缺少 LibreOffice/`soffice` 在转换前失败；OOXML 元数据记录 1 页，但不能当作真实渲染页数或视觉 PASS。下载入口及 DOCX/XLSX 视觉检查的历史证据保留，但本轮后续验证按产品决定 `SKIPPED`。

### 2026-08-19 产品增量 Review

- 检查项适用性：后端正式设备集合已补齐 `station_facility`、`cod_analyzer`，与前端规则选项及站点档案正式代码一致；定向后端 10/10，代码 Review `PASS`。设备适用规则退役后的 Web 设置页、模板弹窗和检查项弹窗均不再显示设备规则、类型匹配或高中低频次，周/月为中文，真实 UI `PASS`；未执行保存、删除或审批，计划 match 无只读 UI 入口，由服务端和生成测试证明。
- 到期巡检建议退役：产品确认固定站点和固定例行工作无需系统主动提醒。后端旧 GET/POST 返回稳定 410 且零写入，调度不再注册、不再新建 `inspection_due_suggestion`；专用候选实现和失效测试已删除。Web/小程序不再展示或请求到期建议；系统性异常复查、站点优先级建议、常用计划和历史通知均保留。后端定向及相邻回归 20/20，小程序 Node 5/5，React Node 18/18、lint/build PASS。
- 小程序计划页：“新建排程”保持筛选下方正常流入口，不固定悬浮；管理员的本人/团队切换现在只作用于系统性异常复查，不影响正常计划列表。
- 小程序日期编辑器真实 UI：万松登录成功。新建周检初始范围为 `2026-08-24` 至 `2026-08-30`，显示 7 个日期行且无逐日添加入口；结束日改为 `2026-08-31` 后自动新增第 8 行，证明周检由日期范围驱动。切换季检后，`2026-10-01` 至 `2026-12-31` 初始为 0 个执行日，只显示“选择执行日期”，未铺开 92 个空日；选择一次后仅新增 `2026-10-01` 一日。退出时未保存、未提交，既有计划仍只有原两张，无业务写入，控制台 0 error，`PASS`。
- 小程序日期编辑器控制台中的 `getSystemInfo`、SharedArrayBuffer、灰度基础库和 worker `reportRealtimeAction` 均为微信开发者工具兼容 warning，不计为业务错误。
- 小程序计划列表状态筛选真实 UI：两张已通过但尚未完成的周检（`2026-08-31` 至 `2026-09-06`、`2026-08-24` 至 `2026-08-30`）切到“进行中”均可见；切到“已完成”显示“暂无排程计划”，两张均未被误归为已完成。过程无业务写入，控制台无业务 error，`PASS`。
- 到期巡检建议退役真实 UI：万松账号“小程序 → 我的计划”直接显示两张实际计划；常规到期建议、本人/团队到期建议切换及生成草稿动作均已消失，异常复查建议继续保留，控制台无新增业务错误，`PASS`。
- 无效计划清理：代码 Review `PASS`；开发定向 25/25、直接相邻 1/1、产品关键 2/2。开发线最新 `5000` 健康；真实 Web 弹窗可正常打开、无报错、空态正确且确认按钮禁用，未执行清理。真实库无自然候选，排程人不存在、停用或失去运维角色的具体原因卡片文案 `NOT RUN`，不为验收造数据。
- 本地凭据：已按安全门禁完成本地凭据迁移并撤销旧会话，重新登录成功；台账不记录密码。
- 候选边界：原 50 个 tracked 内容 diff 加 8 个必要未跟踪实现/测试；门禁修正新增 tracked `backend/test_multirole_cross_module_access.py`，本次纳入此前未修改的 `docs/RELEASE_TEST_HANDOFF.md`，最终为 52 个 tracked 内容 diff 加 8 个必要未跟踪文件，共 60 个候选文件。`miniprogram/services/maps.js` 虽为状态 M 但无内容 diff，根 `project.private.config.json` 明确排除；`python-docx` 是既已授权的唯一新增依赖。

### 2026-08-18 计划 #52 真实 UI 兼容验证

- 环境为开发线小程序/React 客户端连接线上 r9 API，仅证明新客户端对 r9 的兼容；不是候选前后端成对验收。唯一操作对象为测试计划 #52（2026-08-31 至 2026-09-06，4 天/3 站，备注“Codex真实UI验证临时计划”）。
- 小程序发起变更后未改排程，提交成功进入 `change_submitted`/变更待审 v4，原因与提交时间正确，既有 4 个任务保留。Web 统一审核仅出现 #52，路线为 4 日期/3 站且原因正确；顶部“涉及站点”误显示 1。
- Web 以“Codex真实UI验证完成，驳回变更以恢复原计划”驳回成功，待办移除；小程序重新进入后恢复已通过，4 天/3 站及既有 4 个任务无丢失、无重复。#52 当前为 `approved`，审计和通知保留，不再操作。
- 混合版本 Web 因 r9 未返回 `generated_site_tasks` 而误报 0 任务，小程序 r9 fallback 仍把旧执行包显示为“执行中”；两者均不能作为候选结果。由此修正的日期×站点任务展示、审核唯一站点统计、总体 `execution_status` 及列表固定一次批量聚合已通过上述产品独立复验，并由 #990800/#990801 完成开发线前后端成对真实 UI 验收。该兼容证据保留，不再要求操作 #52。

## 当前断点

1. 唯一新候选 `release-20260819-cross-module-freeze-r10` 元数据已同步，产品只读 Review `PASS`，产品独立复跑 `backend.test_release_candidate_gate` 2/2 `PASS`；manifest 仅接受 r10，r1-r9 均为历史候选并逐个拒绝，历史标签不得移动。
2. r10 最终自动门禁 `PASS`；候选共 60 个文件，即 52 个 tracked 内容 diff 加 8 个必要未跟踪文件。`miniprogram/services/maps.js` 无内容 diff、根 `project.private.config.json` 排除。
3. 精确暂存 60 文件及 cached diff、candidate guard、Git index 边界检查均 `PASS`，当前已形成冻结候选内容边界；不可变提交和新标签尚未创建。
4. 唯一下一步是等待用户明确授权生成一个不可变提交与一个新标签；仍不打包、不推送、不部署。
5. 下载入口及 DOCX/XLSX 视觉检查本轮 `SKIPPED`；无效计划清理中三类排程人原因卡片文案 `NOT RUN`。不得将这些结论表述为八项整体 UI 全通过、可部署或已部署。

## 当前禁止事项

- 不把混合环境、自动测试或本轮 `SKIPPED` 的下载和文档视觉检查冒充真实 UI PASS；
- 不提交、暂存、打标签、打包、推送、部署或操作生产/固定数据库/现有服务；
- 不新增 `python-docx` 以外的依赖，不重写报告生成；
- 不清理或纳入 private、TEST_UI、automation、minitest、临时渲染证据和历史源码包。
