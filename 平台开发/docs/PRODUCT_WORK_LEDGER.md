# 产品工作台账

> 更新时间：2026-08-15
> 用途：记录当前主线、断点、责任人和证据。它是可更新的工作状态，不是长期业务规范或发布证明。

## 当前主线

r7 已完成代码 Review 和最终自动门禁；当前候选真实 UI 经产品明确授权记为 `NOT RUN`，不再探索新修正。唯一下一断点为：冻结唯一候选 -> 取得新备份 -> 部署 -> 有限线上 smoke 与使用方反馈监控。

目标候选标签：`release-20260815-cross-module-freeze-r7`。r6 及 r1-r5 仅保留历史追溯，不移动、不复用。

## 固定职责

- 产品主任务：维护问题定义、产品口径、范围、台账、只读 Review、验收边界和发布建议。
- 开发经理：按授权范围实施最小代码修改和测试，不自行改变产品口径或执行版本操作。
- 使用方：在部署后的实际业务中验证已明确登记为 `NOT RUN` 的交互边界并反馈可复现问题。

## 七组状态

| 范围 | 代码与自动验证 | 历史真实 UI 事实 | r7 当前候选 UI |
| --- | --- | --- | --- |
| 站点监测大屏 | 图标、切站、卡片关闭、真实监测设备、无假曲线、关注持久化已实现并纳入相关回归 | Web 1440/1024 曾验收通过 | `NOT RUN`（产品授权跳过） |
| 工单 | 失败反馈、弹窗布局、现场权限、证据写入回滚和安全删除已实现并纳入相关回归 | Web 1440/1024 曾验收通过 | `NOT RUN`（产品授权跳过） |
| 巡检计划 | 草稿编辑、版本冲突、资源裁剪和异步请求隔离已实现并纳入相关回归 | Web 编辑保存与筛选离页曾验收通过 | `NOT RUN`（产品授权跳过） |
| 站点全景 | sites 专用填高、窄屏滚动和空白修正已实现并纳入相关回归 | Web 1440/1024 曾验收通过 | `NOT RUN`（产品授权跳过） |
| 影像档案与统一审核 | 当前/历史互补、业务审核归口、补充材料和统计口径已实现并纳入相关回归 | Web 档案与审核路由曾验收通过；隔离旧文件缺失属于数据边界 | `NOT RUN`（产品授权跳过） |
| 小程序唯一入口、登录与角标 | 根目录唯一入口、生产 API 默认值、登录后角标刷新和角色入口已实现；Node 回归通过 | 唯一入口登录页截图能力曾恢复 | `NOT RUN`（产品授权跳过） |
| 签到一致性与返场补拍 | 同站签到事实、500 米返场门禁、一次性相机会话、水印来源、轮次冻结和失败零副作用已实现并纳入后端/小程序回归 | 曾验证相机拉起与取消、离站状态和单项补拍边界；未形成完整候选验收 | `NOT RUN`（产品授权跳过） |

历史产品口径继续有效：审核不阻塞离站；驳回只重开受影响检查项；返场补拍验证当前位置但不重复整站签到；不引入处理前/处理中/处理后状态机；无归属照片只作补充材料，不进入主审核或正式证据；不实现工单改派/改站。

## r7 自动门禁

- Backend unittest discover：310/310，PASS；`TEST_MEDIA_FIX_notification_failure` 和 work-order flag HTTP 500 为主动故障注入，回滚断言通过。
- 小程序 Node：32/32，PASS。
- React：`test:api` 41/41、cockpit 2/2、lint、production build 均 PASS；build 为 1741 modules。
- Python/JavaScript 语法聚合：136 files，PASS。
- 备份脚本：3/3，PASS。
- Candidate guard：2/2，r1-r6 拒绝、r7 接受。
- `git diff --check`：退出码 0，仅 LF -> CRLF 提示。

完整发布证据和边界记录在 `docs/RELEASE_TEST_HANDOFF.md`。当前候选 UI 只能记为 `NOT RUN`，不得把历史验收或自动测试写成 r7 UI PASS。

## 生产预检与备份断点

生产只读预检已确认：`ops.hhyc-tec.cn/api/health` 为 200，当前 r6 容器 running/healthy，证书有效至 2026-10-28，`/opt` 所在盘可用 9.6 GB，当前发布记录为 r6。该结果不代表 r7 已部署。

生产 `water-monitor-backup.timer` 在 8 月 13 日至 15 日因已安装脚本使用过时默认目录而连续失败。故障已定位，仓库源码修复及隔离脚本测试 3/3 PASS。服务器安装修正脚本和取得新的数据库、uploads 备份仍属于部署前步骤；必须验证 SHA256，任一步失败即停止部署。

## 当前断点

不再扩展代码或 UI 探索。按以下顺序完成唯一发布主线：

1. 将本文档纳入唯一不可变提交，生成 annotated r7 标签和从该标签生成的源码包；
2. 在服务器安装候选内修正后的备份脚本，取得并校验新的数据库与 uploads 备份；
3. 部署 r7，保留 r6 目录、镜像和部署前数据备份；
4. 执行健康检查、数据库完整性检查和有限公网 smoke；任一失败立即回滚；
5. 监控实际使用方对小程序返场、登录/角标及 Web 七组回归的反馈。

## 最终未完成项

- 冻结不可变 commit、annotated tag 和 tag-derived source package；
- 服务器安装修正后的备份脚本并取得、校验新备份；
- 部署 r7；
- 完成健康检查、数据库完整性检查和有限公网 smoke；
- 收集并监控实际使用方反馈。
