# 小程序计划相邻返修交接

> 状态：`CODE_REVIEW_PASS / REAL_UI_NOT_RUN`
> 当前写入：已停止；不提交、不推送、不冻结、不部署。

## 已确认结果

- 周计划附加月/季项并保留真实频次、动态必填与首错定位、照片数量、计划原因审计、300 米围栏、全角色有权站点位置校准均已落地。
- 微信平台使用预警、待审核、审核结果三条独立模板；订阅模板预加载、用户点击直接授权、openid 绑定、待审核/审核结果 outbox 与失败分类代码链已闭环。
- 新建计划已使用稳定创建意图；响应丢失、自动重试、离线重放和人工原样重试返回同一计划，不重复通知或 outbox，同键不同载荷拒绝。
- 历史任务快照不改写，年检与既有月/季计划保持可读可执行。

## Review 证据

- 前轮：后端定向 174 项、小程序 Node 50 项通过。
- 本轮返修：计划与资源后端 66 项、微信订阅后端 18 项、小程序相关 21 项通过。
- Python/JavaScript 语法及范围内 `git diff --check` 通过。
- 真实 UI、体验版真机订阅授权与微信收件：`NOT RUN`。

## 唯一下一动作

形成包含本轮前后端改动的可测试版本，由真实 UI 验收使用体验版真机完成：订阅授权、openid 绑定、审核人待审核收件、提交人审核结果收件及消息落地页验证。线上 `r15` 未包含本轮改动，不能作为本轮功能验收依据。
