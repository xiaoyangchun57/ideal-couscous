# B 领域工作台：巡检与现场执行

> 负责人：协作者 B
> 当前状态：`FIRST_ROUND_WEB_CLOSEOUT`
> 当前分支：`collab/b-web-closeout`
> 停点：完成当前 PR 并等待 Review；不部署、不操作生产数据

## 1. 领域使命

让巡检从计划编制到现场闭环可执行、可追溯、不过度增加一线负担：计划明确，资源可用，到站可信，检查项和影像完整，审核与整改各有独立状态，历史档案能够复核。

B 对本领域承担产品、后端、Web、小程序、数据、测试和后续路线责任，不只负责巡检页面。

## 2. 核心链路

`检查项模板 → 计划编制/路线与资源 → 审批 → 执行包 → 出发准备 → 到站 → 检查与影像 → 提交 → 审核 → 整改包 → 完成与档案`

直接用户包括计划人员、现场运维人员和审核人员。完成标准必须覆盖无计划、资源不足、位置失败、弱网、重复提交、退回整改和历史查看。

## 3. 代码与验证地图

- 后端：`平台开发/backend/app.py` 中 `/api/inspection-v2/*`、巡检附件、审核及计划资源关联。
- Web：`平台开发/react-vite/src/pages/plan-schedules/`、`inspection-settings/`、`audit/`、`archive/`。
- 小程序：`pages/plan/`、`plan-detail/`、`plan-edit/`、`inspection/`、`review/`。
- 重点测试：计划检查项、现场执行、到站、出发资源、证据审核、整改和计划状态相关测试。

开始新事项前必须从计划配置追踪至现场结果和审核档案，不能只验证某个表单能提交。

## 4. 不可变规则

- 审批、现场执行、资源、证据审核和整改状态分轴保存，不能互相推断。
- 当前计划主链路是 `plan-schedules`；旧周计划只作兼容，不得重新作为权威来源。
- 周检默认，月检和季检按需组合成同一计划；检查项和照片要求由权威模板提供。
- 到站、签到、提交、审核和整改必须由服务端重读权限、位置、状态和版本。
- 影像归属业务对象；无可信来源或无归属材料不计有效证据。
- 整改创建独立整改包，不复活旧车辆、签到、申请或已完成执行包。
- 保存失败必须保留输入，弱网重试使用稳定幂等键。

## 5. 上下游协作

- A 提供站点档案、位置和可用范围；B 负责其在计划、路线和签到中的使用语义。
- C 提供车辆、备件、试剂及异常/工单资源契约；B 负责计划和现场如何申请、占用和归还。
- 主线提供人员权限、审批/消息公共能力和首页入口。
- 跨域功能由最终巡检结果所属负责人完成接线，资源负责人 Review 资源契约。

## 6. 自主经营要求

- 持续检查高频计划和现场路径，优先减少重复填写、错误后丢失和无法恢复。
- 完成当前事项后跑通一次计划到整改/归档的完整链路，校对本工作台代码地图。
- 主动提出三个后续候选，说明用户损失、风险、上下游和最短完成路径。
- 本域低风险事项可自行启动；新状态、权限、主入口或跨域资源规则先交主线裁决。

## 7. 当前事项：Web 收口校准任务

本轮是领域自治切换前的首轮校准，不改变 B 的长期领域归属。目标是收口三项已确认 Web 行为：

- 站点全景保留“最后数据”和“关键时间”。
- 人员退出后不再出现在登录、分配和候选列表中，但历史业务仍可读。
- 中文输入法选词期间，实时搜索不被 URL 回写覆盖。

当前写入权：

- `平台开发/react-vite/src/pages/sites/`
- `平台开发/react-vite/src/pages/users/`
- `平台开发/react-vite/src/hooks/useUrlSyncedSearch.js`
- 对应 React 测试

本轮不修改 `backend/app.py`。若发现服务端缺口，在原 PR 给出精确端点、请求、实际响应和预期，不猜测实现。

验收要求：相关测试、lint、build 通过；真实 Web 覆盖站点列、退出账号候选列表和至少一个中文输入法实时搜索场景；未执行项标记 `NOT RUN`。

## 8. 首轮完成后的领域接管

当前 PR 合并后，B 应独立完成一次只读接管：

1. 跑通新建计划、审批、出发准备、现场检查、提交、审核和整改入口。
2. 列出关键状态和 A/C/主线提供的外部契约。
3. 找出三个仍影响一线效率或业务闭环的问题，形成 `READY/ASSESS/BLOCKED` 候选。

## 9. 下一候选方向

- `ASSESS`：新建计划的检查项组合、站点覆盖、车辆选择和错误定位是否仍形成高操作成本。
- `ASSESS`：现场执行在弱网、返回页面、重复点击和失败重试下能否保留上下文且零重复写入。
- `ASSESS`：审核、整改和影像档案的状态、证据和历史是否在 Web/小程序一致。

候选只是调查方向，B 接管后应基于当前真实证据重排，不得直接把它们当成已确认缺陷。
