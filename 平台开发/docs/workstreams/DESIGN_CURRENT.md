# 设计师当前任务：站点图标复核

> 基线：开始任务时的 `origin/main`；在设计 PR 中记录任务起点 SHA
> 分支建议：`design/station-icon-review`
> 停点：提交并推送设计分支，回传静态 Review；不修改业务 JS、接口或状态机

## 当前任务

将 2026-09-21 18:01 更新的 `tab-site.png`、`tab-site-on.png` 与首页、巡检、告警、我的四组图标并排复核，重点检查真机缩放后的重量、辨认、居中和五 Tab 间距。必要时只调整这两张 PNG。

## 项目协作边界

- 具体需求进入开发前，由产品提供用户、场景、信息、动作、状态和不可变业务边界，再由设计师完成该需求的静态方案。
- 静态 Review 后停止重叠文件写入；开发接线完成后再做实际渲染复核。
- 操作规范、交付标准和经验总结沿用设计师已在编制的成果，本交接不重复定义或要求再次产出。

## 可以直接转发给设计师的话

请从远程最新 `main` 创建 `design/station-icon-review` 分支。本轮只复核最新站点 Tab 图标与另外四个 Tab 的并排和真机效果，必要时仅调整 `tab-site.png`、`tab-site-on.png`。完成后先停在静态 Review；通过后由开发接线，接线完成你再检查实际渲染。不要改 JS、接口、权限、路由和业务状态，也不要与开发同时修改同一文件。操作规范、交付标准和经验总结继续按你已经在做的成果推进，这份项目交接不重复安排。

## 回传

- 修改文件和使用的设计系统依据。
- 图标并排结果及是否建议通过。
- 真实微信/Web 运行效果未验证时明确写 `NOT RUN`。
