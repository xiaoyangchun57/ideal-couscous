# 协作者 C 当前任务：试剂跨站读取与安全维护

> 基线：`011b39667b40556b7409a0e3cbfcb76986d565ca`
> 分支建议：`collab/c-reagent-contract`
> 停点：提交并推送分支，创建 PR，等待主线 Review；不部署、不操作生产数据

## 目标

完成站点 Tab 所需的试剂跨站摘要和通用维护接口，使关注原因、QC 状态、数量、单位和动作能力由服务端权威返回；更换与标定具备正确角色、站点范围、事务和幂等保护。

## 允许范围

- `平台开发/backend/app.py` 中试剂相关端点与共用权限/事务辅助函数
- 新增或修改对应后端测试
- 必要数据库迁移；必须兼容现有数据

本轮 C 独占 `backend/app.py`。不修改小程序页面、React 页面、站点接收器和生产资源。

## 契约与验收

- 以 `平台开发/docs/WEB_SITE_IDENTITY_PLAN_SEED_MOBILE_HOME_OVERVIEW_DEVELOPMENT_HANDOFF_20260921.md` 的试剂章节为完整合同。
- 覆盖管理员、运维、仅审核、零站点、越权、不存在、重复请求、同键异参和事务失败回滚。
- `items` 为多原因去重并集；数量 `0`、单位和待标定/失败不得丢失。
- 回传数据兼容说明、修改文件、测试、迁移影响和残余 `NOT RUN`。
