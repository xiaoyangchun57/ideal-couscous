# PR #4 旧周计划执行人校验：B → C 交接已完成

> 交接方：协作者 B；实施方：协作者 C；原 PR：`collab/b-web-closeout` → `integration`。
> 状态：C 的服务端返修与回传已完成，`backend/app.py` 本轮独占写入权已释放；本文件不再下达 C 的待执行任务。独立真实 UI 验收仍为 `NOT RUN`。

## 已完成的接口合同

- 旧 `POST /api/weekly-plans` 只作兼容；当前权威计划主链路是 `plan-schedules`，本交接不恢复旧页面路由，也不扩大计划权限。
- C 的提交 `83feb67`、`3e37a14` 已集成 PR #4：`user_id` 须为 SQLite 主键范围内的正整数；不存在账号返回 `404` + `PLAN_EXECUTION_USER_NOT_FOUND`，停用或已注销账号返回 `409` + `PLAN_EXECUTION_USER_INACTIVE`。非管理员不能为他人建计划；历史计划仍可读取。
- 服务端在同一写事务中查询执行人并写入周计划/用车申请；拒绝或写入失败时，两表不得新增记录。此前 B 对 C 的代码复审及后端联合回归结果已在 PR #4 评论记录，不把历史结果当成本轮未运行测试的证据。

## 当前写入与验收边界

- C 的本轮任务已闭环，不再占用 `backend/app.py` 写入窗口。B 已在代码基线 `83938f8` 补齐非对象 JSON 请求体返回 400 及隔离 API 测试，并将增量验证写入原 PR #4 正文；本交接不再下达后端写入任务。如后续需要 C 再写该文件，须重新协调独占窗口。
- 当前待原 PR #4 实时头部的独立产品/代码 Review、独立真实 UI 证据或旧页面不可测边界裁定；未完成项明确标记 `NOT RUN`。此前 B 对 C 的代码复审与开发者 Edge/Flask 贯通观测不能替代当前头部的独立 Review 或真实 UI PASS。
- 阶段二“常用计划权限校准”须在 PR #4 收口进入 `integration` 后从最新基线另建分支/PR；不得借此交接修改 `plan-schedules` 权限、UI 设计、新数据库结构或部署配置。
