# 水质运维长期协作基线

> 状态：`ACTIVE_COLLABORATION_BASELINE`
> 代码基线：`011b39667b40556b7409a0e3cbfcb76986d565ca`
> 性质：协作开发基线，不是发布候选，不允许据此部署

## 1. 使用方式

所有参与者从远程 `main` 拉取代码，确认历史包含上述基线提交，再从最新 `main` 创建自己的任务分支。交接只使用仓库相对路径、提交号和 PR，不依赖任何人的本机路径。

本目录固定保留五份当前交接：`A_CURRENT.md`、`B_CURRENT.md`、`C_CURRENT.md`、`MAIN_CURRENT.md`、`DESIGN_CURRENT.md`。每次新任务直接替换对应文件中的已闭环内容，不按日期无限新增文件。历史以 Git 和 PR 为准。

设计规范入口为 `README-design.md`。规范用于约束视觉和交互实现，不能覆盖当前 `CURRENT` 文件中的业务范围、权限、接口、状态迁移或写入边界；发生冲突时，先停止实现并由产品与对应域负责人裁决。

## 2. 全系统长期责任矩阵

| 单元 | 主责业务域 | 完整责任 |
|---|---|---|
| 协作者 A | 站点与监测数据 | 站点档案、身份/MN、接收解析、匹配、有效观测、趋势、地图及双端监测体验 |
| 协作者 B | 巡检与现场执行 | 计划、检查项、到站、影像证据、提交、审核、整改和执行档案 |
| 协作者 C | 异常与运维资源 | 告警、工单、车辆、设备、物资、备件、试剂和资源占用 |
| 我们主线 | 平台与协作基础 | 人员权限、审批、消息通知、首页/我的、公共交互、审计、跨域集成和版本管理 |
| 设计师 | 横向设计系统 | 全系统信息架构、组件/令牌、关键状态、跨端一致性和真实 UI 设计复核 |

主责不是永久独占。每轮每个开发单元至少完成一个本域任务；从第二轮起，再领取一个其他领域的小型完整需求。每个域设主责和轮换备份，避免单点知识。

## 3. 每项任务都必须端到端

开发任务不能只交一张页面或一个接口。任务接收者需要完成：

1. 用用户、场景、损失和完成标准解释问题。
2. 核对真实数据、权限、状态和上下游调用。
3. 在约定范围内完成后端、Web、小程序或必要组合。
4. 补与风险相称的测试，标明真实 UI `PASS` 或 `NOT RUN`。
5. 在自己的分支提交并推送，通过 PR 回传；不得直接覆盖他人分支。

## 4. 分支与集成

- 分支命名：`collab/a-主题`、`collab/b-主题`、`collab/c-主题`、`collab/main-主题`、`design/主题`。
- 一个任务只允许一个写入者。不同分支如需要修改同一高冲突文件，先由主线调整批次顺序。
- PR 必须写：产品结论、修改文件、接口/数据兼容、测试、真实 UI 状态、残余风险。
- 主线负责依赖顺序、代码 Review、冲突整合和合入 `main`；合入不自动形成发布候选。
- 发布候选只在一个集成提交完成代码 Review、必要真实 UI 验收和最终门禁后另行冻结。

## 5. 设计协作

设计师不是开发完成后的美化环节。每个有明显交互或信息架构变化的任务按以下顺序协作：

1. 域负责人先给产品骨架：用户、场景、信息、动作、状态、错误和不可变业务边界。
2. 设计师在 `design/主题` 分支完成页面层级、组件、状态稿和允许的 UI 文件，停在静态 Review。
3. 静态通过后，开发者接线业务数据、接口、权限和失败保护；设计师停止重叠文件写入。
4. 可运行后由设计师检查视觉一致性，独立 UI 验收检查真实行为，二者不能互相替代。

纯接口、数据迁移、权限修复和无视觉变化的缺陷不强制经过设计师。设计系统资产和通用组件由设计师维护，业务开发不得另造第二套样式。

设计规范的阅读顺序：`DESIGN-RULES.md` → 按需查 `COMPONENT-DECISION-TREE.md` → 设计完成后用 `DESIGN-CHECKLIST.md` → 开发接线参考 `DEV-HANDOFF.md` → 回传前按 `DELIVERY-STANDARD.md`。新增公共组件或 Token 才读取 `CONTRIBUTING.md`。

## 6. 当前已知边界

- 站点 Tab 静态结构通过；最新两张图标在上一轮 Review 后更新，待五 Tab 并排和真机复核。
- 小程序第五 Tab、一次性目标、站点/试剂业务接线尚未完成。
- 数据接入暂停等待站点正式名称、MN、有效状态和别名统一；少量未匹配不得阻断其他站点。
- Web 已知收口项和站点 Tab 开发按本目录第一轮交接推进。
- 基线已通过后端相关测试 284 项中的唯一夹具问题修正后单文件 22/22、小程序基线 230/230、React 100/100、React lint/build 和 189 文件语法检查。真实 UI 未因此自动通过。

## 7. 第一轮并行关系

- A、B、C、主线与设计师分别只写各自 `CURRENT` 文件允许的范围。
- C 独占本轮 `backend/app.py` 写入；其他人发现后端缺口先回传，不并发修改。
- 设计师本轮不再改站点页 WXML/WXSS，仅处理站点 Tab 图标复核；其已在编制的操作规范、交付标准和经验总结不在本交接重复安排。
- 合并顺序：C 后端契约 → A 站点页接线 → 主线导航/首页集成；B 的 React 收口可独立合入。设计资产通过后单独合入。
