# A 领域工作台：站点与监测数据

> 负责人：协作者 A
> 当前状态：`PR #1 / CODE_REVIEW_REWORK_REQUIRED`
> 当前分支：`collab/a-station-tab-read`
> 停点：更新原 PR 并等待复审；不新建 PR、不合并、不部署

## 1. 领域使命

让站点身份、接收数据和双端展示形成可信闭环：数据能接收、解析、匹配、保存和展示，用户能区分“正常、需关注、暂无有效数据”，且系统不把仅收到报文误写成设备或仪器健康。

A 对本领域承担产品、后端、Web、小程序、数据适配、测试和后续路线责任，不只负责站点页面。

## 2. 核心链路

`站点档案/MN → 报文接收与原文证据 → 认证和站点匹配 → 规范化观测 → 监测查询/趋势 → Web 与小程序展示 → 异常事实提供给 C`

直接用户包括管理员、运维人员和查看监测结果的审核人员。完成标准必须覆盖数据进入前、等待首帧、已接收但未配置、正常展示、失败重试和无权限。

## 3. 代码与验证地图

- 后端：`平台开发/backend/app.py` 中 `/api/sites`、`/api/data/*`、站点监测与移动站点接口；数据接收相关模块和迁移位于 `平台开发/backend/`。
- Web：`平台开发/react-vite/src/pages/sites/`、`cockpit/`、`analysis/`。
- 小程序：`平台开发/miniprogram/pages/responsible-sites/`、`pages/site/`，站点读取接口位于 `services/api.js`。
- 重点测试：后端站点/接入/监测测试，小程序 `responsibleSitesMonitoring.test.js`、`siteReadonlySource.test.js`，React 站点、驾驶舱和分析页测试。

开始新事项前先从用户入口追踪到接口、数据表和下游消费者；本地图不替代实际代码核对。

## 4. 不可变规则

- MN 是接入身份的第一精确依据，名称和别名只辅助匹配；少量未匹配不得阻断其他站点。
- 当前检测周期统一按 4 小时理解；分钟报文是传输频率，不得冒充检测周期。
- 接收状态、数据有效性、设备状态和仪器状态是不同事实；没有数据来源不得推断 RTU 或仪器健康。
- 原始报文证据与规范化观测分层保存，展示修正不得静默改写原始证据。
- 服务端负责站点范围和监测权限；前端隐藏不构成权限控制。
- 站点位置、名称等档案调整必须考虑 B 的签到/计划和 C 的告警/工单引用。

## 5. 上下游协作

- B 消费站点档案、位置和可巡检范围；相关字段变化由 A 提供兼容契约，B Review 现场影响。
- C 消费有效观测并负责告警、工单和试剂业务；监测事实由 A 定义，处置状态由 C 定义。
- 主线提供人员权限、首页摘要、公共导航和版本集成。
- 跨域变更由最终用户结果所属领域担任结果负责人，不通过用户转述。

## 6. 自主经营要求

- 维护本领域真实现状、问题池和 `READY` 候选，完成当前事项后主动提出三个后续候选。
- 本领域低风险、非破坏性事项可按总入口规则自行启动。
- 涉及公共权限、全局导航、破坏性数据变更或其他领域状态时，先冻结跨域契约。
- PR 必须说明产品判断、完整链路、兼容性、测试、真实 UI 状态和残余风险。

## 7. 当前事项：站点 Tab 读取链

目标是把已完成静态结构的“站点”模式接入真实站点目录和监测读取能力。管理员可切换本人/全部并搜索，其他角色只看授权站点；详情沿用既有路由。

当前写入权：

- `平台开发/miniprogram/pages/responsible-sites/`
- `平台开发/miniprogram/services/api.js` 的站点读取部分
- 对应小程序测试

本轮不修改首页、`app.json`、“我的”、试剂后端或 `backend/app.py`。

### PR #1 唯一返修

监测列表成功展示后，如果监测接口明确返回 `STATION_MONITORING_PUBLIC_DISABLED` 或 `STATION_MONITORING_ADMIN_ONLY`，必须立即关闭 `monitoringEnabled/monitoringPublic`。即使随后目录回退失败，也只能保留旧站点目录和可重试错误，不得继续显示旧监测字段；详情来源切换为档案模式。

补一条“先成功、再门禁 403、再目录失败”的回归。只运行 `responsibleSitesMonitoring.test.js`、相关 JS 语法和 `git diff --check`，在原 PR 更新。

## 8. 当前跨域结果责任

当前“站点 Tab 可完整使用”的结果负责人是 A：

- 主线提供已通过静态 Review 的五 Tab 资产、导航注册和首页 `stationHubTarget` 发起能力。
- C 提供试剂跨站读取和维护契约，并直接与 A 对齐接口。
- A 在 PR #1 通过并取得站点页写入权后，负责消费一次性目标、接入真实试剂契约并完成端到端验证。

## 9. 下一候选

- `READY_AFTER_PR`：接管站点 Tab 最终接线，覆盖首页直达站点/试剂、返回状态保持和失败重试。
- `REVIEW_READY_STATION_MASTER_REFRESH`：已按批准的 38 行站点表形成独立检查点 `collab/a-station-master-refresh`。实现保留历史站点 ID，以 `active/retired` 区分现行目录，按 MN 优先、名称/别名辅助匹配并同步受信端点和监测配置；“坝上”MN 固定为 `62305550`，“蛇山”不再进入有效目录。离线 `plan/apply/verify`、事务回滚、来源指纹和重复执行验证已覆盖，站点相关 119 项测试通过。当前仅提交并推送代码，未连接或改写生产数据库；合入前仍需主线产品 Review，以及 B 对签到/计划引用、C 对告警/工单引用的兼容性 Review。
- `ASSESS`：核对 Web 站点全景、驾驶舱、趋势与小程序站点页是否使用同一监测语义，提出最短收口方案。

完成首轮后，A 需要补充或调整这三个候选的证据和优先级，而不是等待主线重新派单。
