# A 领域工作台：站点与监测数据

> 负责人：协作者 A
> 当前状态：`PR #1/#2 MERGED_IN_MAIN / REAL_UI_NOT_RUN / DOMAIN_ASSESSMENT_ACTIVE`
> 当前分支：`collab/a-station-domain-followup`（从 `main@b6880ac` 派生）
> 停点：主线接收并派发独立真实 UI 验收；A 继续核对站点监测语义与站点档案候选，不直接改公共页面、生产数据或 B/C 业务。

## 1. 领域使命

让站点身份、接收数据和双端展示形成可信闭环：数据能接收、解析、匹配、保存和展示，用户能区分“正常、需关注、暂无有效数据”，且系统不把仅收到报文误写成设备或仪器健康。

A 对本领域承担产品、后端、Web、小程序、数据适配、测试和后续路线责任，不只负责站点页面。

## 2. 核心链路

`站点档案/MN → 报文接收与原文证据 → 认证和站点匹配 → 规范化观测 → 监测查询/趋势 → Web 与小程序展示 → 异常事实提供给 C`

直接用户包括管理员、运维人员和查看监测结果的审核人员。完成标准必须覆盖数据进入前、等待首帧、已接收但未配置、正常展示、失败重试和无权限。

## 3. 代码与验证地图

- 后端：`平台开发/backend/app.py` 中 `/api/sites`、`/api/data/*`、站点监测与移动站点接口；数据接收相关模块和迁移位于 `平台开发/backend/`。
- Web：`平台开发/react-vite/src/pages/sites/`、`cockpit/`、`analysis/`。
- 小程序：`平台开发/miniprogram/pages/responsible-sites/`、`pages/site/`，站点读取接口位于 `services/api.js`。
- 重点测试：后端站点/接入/监测测试，小程序 `responsibleSitesMonitoring.test.js`、`siteReadonlySource.test.js`，React 站点、驾驶舱和分析页测试。

开始新事项前先从用户入口追踪到接口、数据表和下游消费者；本地图不替代实际代码核对。

## 4. 不可变规则

- MN 是接入身份的第一精确依据，名称和别名只辅助匹配；少量未匹配不得阻断其他站点。
- 当前检测周期统一按 4 小时理解；分钟报文是传输频率，不得冒充检测周期。
- 接收状态、数据有效性、设备状态和仪器状态是不同事实；没有数据来源不得推断 RTU 或仪器健康。
- 原始报文证据与规范化观测分层保存，展示修正不得静默改写原始证据。
- 服务端负责站点范围和监测权限；前端隐藏不构成权限控制。
- 站点位置、名称等档案调整必须考虑 B 的签到/计划和 C 的告警/工单引用。

## 5. 上下游协作

- B 消费站点档案、位置和可巡检范围；相关字段变化由 A 提供兼容契约，B Review 现场影响。
- C 消费有效观测并负责告警、工单和试剂业务；监测事实由 A 定义，处置状态由 C 定义。
- 主线提供人员权限、首页摘要、公共导航和版本集成。
- 跨域变更由最终用户结果所属领域担任结果负责人，不通过用户转述。

## 6. 自主经营要求

- 维护本领域真实现状、问题池和 `READY` 候选，完成当前事项后主动提出三个后续候选。
- 本领域低风险、非破坏性事项可按总入口规则自行启动。
- 涉及公共权限、全局导航、破坏性数据变更或其他领域状态时，先冻结跨域契约。
- PR 必须说明产品判断、完整链路、兼容性、测试、真实 UI 状态和残余风险。

## 7. 当前事项：合入后领域接管与语义核对

C PR #2、A PR #1 已按顺序进入 `main@b6880ac`；A 原 PR 的站点 Tab 导航目标消费、监测门禁及试剂消费接线已成为主线基线。合入不代表真实 UI、真实监测接入、冻结或部署通过。主线已在协作群确认真实 UI 仍 `NOT RUN`、另行安排；A 提供独立验收输入 `A_STATION_REAL_UI_ACCEPTANCE_HANDOFF_20260924.md`，等待主线接收并派发，不自行给主线的独立 UI 任务下验收结论。

当前唯一写入者：A 仅写本领域 `A_CURRENT.md`、上述验收交接及必要的**只读核对结论**；本分支不修改业务代码、首页、公共导航、`backend/app.py`、B/C 业务或生产站点。发现实际缺陷后先核对消费契约与写入窗口，再在单独范围内修复并由受影响领域 Review。

### 只读核对：站点语义不能直接混用

- 小程序站点 Tab 从 `miniprogram/pages/responsible-sites/` 经 `services/api.js` 读取 `/api/station-monitoring/sites` 与单站 overview；服务端权限与 `station_monitoring_public` 决定是否展示监测字段。
- Web 站点页的站点/监测入口位于 `react-vite/src/pages/sites/`；驾驶舱 `CockpitPage.jsx` 同时读取 `/sites`、`/devices/monitoring-summary`、`/data/health`，站点图标可受告警、设备离线和设备最近数据时间影响；分析页 `AnalysisPage.jsx` 的到报汇总和趋势分别来自 `/data/arrival/summary`、`/analysis/trends`。
- **判断**：这些是不同来源、不同问题的信号；当前只证明读取路径不同，尚未证明 Web 与小程序存在实现缺陷。下一步须在隔离环境逐一对应“报文到达/有效观测/设备离线/告警”，并在跨页统一文案或颜色前由 A 给出映射与 B/C 消费边界。不得仅因某站点有最近数据就把设备/仪器健康标为正常。

## 8. 上下游和验证状态

- A 保持“站点身份→监测事实→双端展示”的结果责任；B 消费站点位置/档案参与现场计划，C 消费有效观测并负责试剂、告警/工单；主线拥有公共导航、最终集成裁决及独立真实 UI 的派发。
- 2026-09-24 基于 `main@b6880ac` 的定向回归：小程序监测/只读来源/站点导航/本地 API 四个测试文件报告 5 项通过；Web 站点监测接线与试剂删除两个文件报告 10 项通过。隔离临时数据库的健康和本地管理员登录通过，但没有真实站点观测、普通角色交互或真实微信/网页业务 UI 证据。
- 微信真实 UI、Web 真实 UI、真实监测接入端到端均 `NOT RUN`。独立 UI 验收另见交接文件；完成前不可声称功能整体通过、冻结或可部署。

## 9. READY 候选与唯一下一动作

1. `READY / 只读核对`：逐页追踪 Web 站点全景/驾驶舱/分析与小程序站点页的指标来源、权限及状态口径，列出是否真实矛盾、最小改动及受影响的 B/C 消费方；本轮已形成上述初步代码地图，下一步补隔离响应样本并提出收口判断。
2. `WAITING_STATION_MASTER_REFRESH`：核对正式站点名称、MN、有效状态、别名与接入匹配。仅在主线确认正式来源、样本与数据纠错边界后做隔离验证，不批量改固定库或生产数据。
3. `READY_AFTER_UI_HANDOFF`：独立真实 UI 回传后由 A 分析站点领域缺陷和复现路径；视严重度开定向修复，不能以交接已发送视为 UI PASS。

**唯一下一动作**：主线接收 UI 交接并派发独立验收；A 继续候选 1 的只读语义核对，不抢写 B/C 或公共页面。群消息接棒须等下一行动人确认。
